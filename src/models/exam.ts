export type ExamScheduleItem = {
  dateLabel: string;
  dayLabel: string;
  icon: string;
  iconColor: string;
  iconTint: string;
  id: string;
  room: string;
  subject: string;
  timeLabel: string;
  trackColor: string;
};
